# =============================================
# Smart Society Management System
# application.properties
# =============================================

# Server
server.port=8080
spring.application.name=smart-society

# ---- MySQL DataSource ----
spring.datasource.url=jdbc:mysql://localhost:3306/smart_society_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vaibmore@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---- JPA / Hibernate ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---- JWT ----
app.jwt.secret=SmartSocietySecretKey2024VeryLongSecretKeyForJWTTokenGeneration
app.jwt.expiration-ms=86400000

# ---- Swagger ----
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# ---- Logging ----
logging.level.com.society=DEBUG
logging.level.org.springframework.security=INFO

# ---- CORS ----
# localhost for browsing from this machine, plus common private LAN ranges
# (192.168.x.x, 10.x.x.x) so phones/other laptops on the same Wi-Fi/router
# can reach the API when you open the frontend via this machine's LAN IP.
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://192.168.*.*:5173,http://10.*.*.*:5173

# --- RazorePay ----
razorpay.key.id=rzp_test_TIE6afBy8Ym1vU
razorpay.key.secret=TS56USyZKN67szcxVYdoBjiM
razorpay.currency=INR

# ---- Email (Forgot Password) ----
# Gmail example: create an "App Password" at https://myaccount.google.com/apppasswords
# (needs 2-Step Verification turned on first) - do NOT use your normal Gmail password here.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=samrtsociety07@gmail.com
spring.mail.password=yzrq pyrt tesb ervj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Link the reset email points to (your frontend's reset-password page)
app.frontend.reset-password-url=http://localhost:5173/reset-password

# Reset token validity in minutes
app.reset-token.expiry-minutes=15
